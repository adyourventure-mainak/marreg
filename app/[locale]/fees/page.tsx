import {Page} from "../../../components/Shell";
export default async function Fees({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <Page locale={locale} eyebrow="Fees and payments" title="Know the cost before you apply." lede="Fees depend on the governing act and registrar. The figures below are illustrative demo data and must be confirmed with your office."><div className="mt-10 max-w-2xl overflow-hidden border border-rule bg-surface"><div className="grid grid-cols-2 border-b border-rule bg-teal p-4 text-xs font-bold uppercase tracking-widest text-white"><span>Service</span><span>Published fee</span></div>{[['Marriage registration','৳ 500'],['Certified copy','৳ 100'],['Correction request','৳ 200']].map(x=><div key={x[0]} className="grid grid-cols-2 border-b border-rule p-5 text-sm last:border-0"><span>{x[0]}</span><strong>{x[1]}</strong></div>)}</div></Page>}
