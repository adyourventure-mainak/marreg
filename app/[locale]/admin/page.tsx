import {Page} from "../../../components/Shell";
export default async function Admin({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <Page locale={locale} eyebrow="Administration" title="Keep the service accurate." lede="Manage published guidance, office records, and the review queue. Administrative actions are mocked for this frontend build."><div className="mt-10 grid gap-4 md:grid-cols-3">{[['Content review','3 items awaiting publication'],['Office directory','482 active registrar offices'],['Audit log','All changes are recorded']].map(x=><div key={x[0]} className="border border-rule bg-surface p-6"><h2 className="text-2xl">{x[0]}</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{x[1]}</p><button className="focus mt-6 border-b-2 border-saffron pb-1 text-sm font-bold text-teal">Open workspace →</button></div>)}</div></Page>}
